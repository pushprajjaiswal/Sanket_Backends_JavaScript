// data types in javascript
// primitive data type: number, string and boolean.
let num = 10;
console.log(typeof num);
num = 'Hello';
console.log(typeof num);
// object data type: array and function are also objects.
//  Compostion of other types of data
//To store Key Value Pairs Use the Objects.
//    <Key, value> Pairs 
// {
    // name: pushpraj;
    // company: Google;
    // Position: SDE1;
// }
// Keys will be Unique





let arr = [1,2,3];
console.log(typeof arr);
function add(a,b){
    return a+b;
    }
    console.log(typeof add)
    /* null & undefined */
    let nulVal=null;
    let undefVal=undefined;
    console.log(nulVal === null);
    console.log(undefVal===null);
    console.log(undefVal===undefined);
    /* Symbol is introduced in ES6 which is used to create unique key for an object property.*/
    let symKey=Symbol("key");
    console.log(symKey);
                     /* NULL Vs Undefined */
    /* Null represents absence of any value or data. It’s like saying “I don’t know anything about this”. Whereas Undefined means that a variable has been declared but not initialized with

// Special Characters in javascript
/* \' - single quote
* \" - double quotes
*\t - tab
*\r - carriage return
*\v - vertical tab
*\f - form feed
\` - backtick (grave accent)
\\ - backslash
*/
/* Template Literals are introduced in ES6 and they have two types of interpolation.
* 1. Interpolation : It is when we want to include the value of a variable inside a string literal.
* 2. Multiline strings : When you need to write a multiline string with embedded expressions.
*/
let age = 10;
let name = "pushpraj";
console.log(name,"\n", age);
//  comments in javasript 
/* Single line comment starts from // till end of the line.
* Multi-line comments start from   /* and ends at */

